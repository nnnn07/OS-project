# OS-project
Secure File Sharing System Using Cryptography is a desktop application that encrypts files using AES-256 before storage or sharing. It ensures only authorized users can decrypt and access files, protecting sensitive data. The project reflects key Operating System concepts like file handling, I/O operations, and security management.
